function adicionarAoBalance(balance, valor) {
    return balance + valor;
  }

function subtrairDoBalance(balance, valor) {
  return balance - valor;
}

function multiplicarBalancePorTaxa(balance, taxa) {
    return balance * taxa;
  }

function dividirBalance(balance, divisor) {
  return balance / divisor;
}

